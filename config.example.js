// config.js 템플릿 (이 파일은 레포에 올라감 / 실제 키는 config.js 에 둠)
// 사용법: 이 파일을 config.js 로 복사한 뒤 Supabase 값을 채운다.
//   cp config.example.js config.js
// Supabase 대시보드 → Settings → API 에서 복사.
window.SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY"; // anon public 키 (service_role 절대 금지)
